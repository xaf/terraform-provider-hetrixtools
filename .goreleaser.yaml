version: 2

project_name: hetrixtools

builds:
  - id: terraform-provider
    main: ./terraform-provider
    binary: terraform-provider-hetrixtools_v{{ .Version }}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{ .Version }}

archives:
  - id: terraform-provider
    ids:
      - terraform-provider
    formats:
      - zip
    name_template: "terraform-provider-hetrixtools_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - none*

checksum:
  name_template: "terraform-provider-hetrixtools_{{ .Version }}_SHA256SUMS"

signs:
  - artifacts: checksum
    args:
      - --batch
      - --yes
      - --pinentry-mode
      - loopback
      - --passphrase
      - "{{ .Env.GPG_PASSPHRASE }}"
      - --local-user
      - "{{ .Env.GPG_FINGERPRINT }}"
      - --detach-sign
      - --output
      - ${signature}
      - ${artifact}

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
